name: Deploy GitHub Pages
# Disabled: Actions deploy failed at configure-pages.
# Use Settings → Pages → Deploy from a branch → main → / (root)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Branch-based Pages only — see README"
